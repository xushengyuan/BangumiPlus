import urllib
import json
import os
SettingsPath='.\\settings.json'
def GetBangumiName(id):
    IdData={'_ids':id}
    IdJson=json.dumps(IdData) 
def ImportSettingns:
    0.
    SettingsFile=open(settings_path,"r+")
    SettingsJson=json.load(SettingsFile.read())
def main:
    
main()